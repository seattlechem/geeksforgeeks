class Solution {

}

incrementally add chars to different buckets, moving in a pattern first -> last
-. first
Then concatenate the contents of each buckets
Then concatenate all of the bucket results into a final string
